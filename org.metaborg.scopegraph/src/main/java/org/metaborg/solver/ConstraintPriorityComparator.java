package org.metaborg.solver;

import java.util.Comparator;

import org.metaborg.solver.constraints.IConstraint;

public class ConstraintPriorityComparator implements Comparator<IConstraint> {

    @Override public int compare(IConstraint first, IConstraint second) {
        return 0;
    }

}
