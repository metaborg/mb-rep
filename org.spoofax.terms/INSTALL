
To build the binary jar file into target/:

 # ant -f build.ant.xml jar

To build the source tarball into target/:

 # ant -f build.ant.xml dist

To build the binary tarball into target/:

 # ant -f build.ant.xml bindist

